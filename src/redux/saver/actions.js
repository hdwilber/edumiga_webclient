import { createActionLabels } from '../utils'
export const PUSH = createActionLabels('SAVER_PUSH')
export const FINISHED = createActionLabels('
const SavingState = {
  saving: 'saving',
  saved: 'saved'
}

const function runSaving(info) {
  const { instance, children } = info
  if (instance) {

  }

}
